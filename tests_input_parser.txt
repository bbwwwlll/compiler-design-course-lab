{
  // ===== 正确部分：应当 Parse OK，并输出推导序列 =====

  x = 1 + 2 * 3;
  y = (1 + 2) * 3;
  z = -x + +y * 2;

  if (x >= 5) {
    y = y + 1;
  } else {
    y = y - 1;
  }

  if (y != 0)
    z = z + 10;

  while (z > 0)
    z = z - 1;

  while (z == 0) {
    if (x <= 10) {
      x = x + 1;
    } else {
      x = x - 1;
    }

    if (x < 20) {
      y = y + 2;
    }

    if (x >= 20) {
      z = 1;
    }
  }

  // ===== 故意出错部分 1：缺少分号 ';' =====
  // 预期：parser 在这里报错，提示 expected SEMI（或 expected ';'）
  a = 1 + 2

  // ===== 故意出错部分 2：括号不匹配，缺少 ')' =====
  // 预期：parser 在匹配 ')' 时报错
  if (a >= 3 {
    b = 0;
  }
}
