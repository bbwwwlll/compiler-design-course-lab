// ===== Lexer Test Input (with intentional error at end) =====
// 目的：覆盖关键字、标识符、整数、所有运算符、分隔符、注释、空白处理，并测试词法错误定位

{
  // --- 正确部分：应完整输出 token 序列 ---

  _x = 0;
  abc123 = 42;

  y = _x + abc123 - 5 * 2 / 1;

  if (y == 42) { z = 1; }
  if (y != 42) { z = 2; }
  if (y <  100) { z = 3; }
  if (y <= 100) { z = 4; }
  if (y >  10)  { z = 5; }
  if (y >= 10)  { z = 6; }

  while (z != 0) z = z - 1;

  /* 块注释测试：
     == != <= >= + - * / ( ) { } ; ,
     lexer 应当跳过
  */

  a, b, c;
  t    =     (  3 + 4 ) *  (2 - 1)  ;
}

// --- 故意出错部分：词法错误（非法字符） ---
// 预期：lexer 在 '@' 处报错，输出文件中给出行列号
bad_token = 123 @ 456;
